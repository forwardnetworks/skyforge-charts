{{- if .Values.skyforge.dns.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: technitium-dns
  labels:
    app: technitium-dns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: technitium-dns
  template:
    metadata:
      labels:
        app: technitium-dns
    spec:
      automountServiceAccountToken: false
      containers:
        - name: technitium-dns
          image: {{ .Values.images.technitiumDns | quote }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: web
              containerPort: 5380
            - name: dns-udp
              containerPort: 53
              protocol: UDP
            - name: dns-tcp
              containerPort: 53
              protocol: TCP
          env:
            - name: DNS_SERVER_DOMAIN
              valueFrom:
                configMapKeyRef:
                  name: skyforge-config
                  key: SKYFORGE_HOSTNAME
            - name: DNS_SERVER_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: skyforge-admin-shared
                  key: password
          volumeMounts:
            - name: data
              mountPath: /etc/dns
          livenessProbe:
            httpGet:
              path: /
              port: 5380
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 3
          readinessProbe:
            httpGet:
              path: /
              port: 5380
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
          resources:
{{ toYaml .Values.skyforge.dns.resources | indent 12 }}
      volumes:
        - name: data
{{- if .Values.skyforge.dns.persistence.enabled }}
          persistentVolumeClaim:
            claimName: technitium-dns-data
{{- else }}
          emptyDir: {}
{{- end }}
{{- if .Values.skyforge.dns.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-dns-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.skyforge.dns.persistence.size | quote }}
{{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: technitium-dns
  labels:
    app: technitium-dns
spec:
  type: ClusterIP
  selector:
    app: technitium-dns
  ports:
    - name: web
      port: 5380
      targetPort: 5380
      protocol: TCP
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
{{- end }}
