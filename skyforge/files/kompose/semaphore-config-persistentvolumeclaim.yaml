apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: semaphore-config
  name: semaphore-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
