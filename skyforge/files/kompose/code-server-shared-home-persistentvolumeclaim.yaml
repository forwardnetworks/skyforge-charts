apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: code-server-shared-home
  name: code-server-shared-home
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
