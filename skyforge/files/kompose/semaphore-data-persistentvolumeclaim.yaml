apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: semaphore-data
  name: semaphore-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
