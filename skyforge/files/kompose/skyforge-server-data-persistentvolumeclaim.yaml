apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: skyforge-server-data
  name: skyforge-server-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
