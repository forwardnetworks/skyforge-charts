apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: nautobot-static
  name: nautobot-static
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
