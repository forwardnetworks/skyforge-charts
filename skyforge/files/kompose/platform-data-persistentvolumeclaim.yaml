apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: platform-data
  name: platform-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
