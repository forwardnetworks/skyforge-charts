apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/name: gitea-data
  name: gitea-data
spec:
  accessModes:
    - {{ default "ReadWriteOnce" .Values.skyforge.gitea.storage.accessMode | quote }}
{{ with .Values.skyforge.gitea.storage.storageClassName }}
  storageClassName: {{ . | quote }}
{{ end }}
  resources:
    requests:
      storage: {{ default "5Gi" .Values.skyforge.gitea.storage.size | quote }}
