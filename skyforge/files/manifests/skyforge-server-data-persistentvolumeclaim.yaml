apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app.kubernetes.io/name: skyforge-server-data
  name: skyforge-server-data
spec:
  accessModes:
    - {{ default "ReadWriteOnce" .Values.skyforge.skyforgeServerData.storage.accessMode | quote }}
{{ with .Values.skyforge.skyforgeServerData.storage.storageClassName }}
  storageClassName: {{ . | quote }}
{{ end }}
  resources:
    requests:
      storage: {{ default "5Gi" .Values.skyforge.skyforgeServerData.storage.size | quote }}
